// Criando um array, chamado lista
let lista = [];

lista.push('maça')
lista.push('pera')
lista.push('banana')
lista.push('morango')

//adicionando elemento no array
//console.log(lista)

//verificar a quantidade de itens
//console.log(lista.length)

// Iterar a lista, ou seja, percorrer cada item
lista.forEach(function(item) {
    console.log(item)
})


